//Function to calculate sum
 const sum = (a, b) => { 
   return a + b 
}
 console.log(sum(9, 7));

//Function to calculate difference
const diff = (c, d) => {
    return c - d;
}
console.log(diff(5, 2));

//Function to multiply two nums
const mul = (p, q) => {
    return p * q;
}
console.log(mul(5, 2));

//Function to divide two nums
const dvd = (x, y) => {
    return x / y;
}
console.log(dvd(10, 2));